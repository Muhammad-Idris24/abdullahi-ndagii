# Personal
TODO.txt
_thumbs.db

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*

# Python
__pycache__/
*.py[cod]
*.egg-info/
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg
.PROJECT
.PYTHONSTARTUP
*.manifest
*.spec
.PYTHONSTARTUP.python-version
.pybuilder*.spec
.ipynb_checkpoints

# Env / virtualenv
.venv/
venv/
ENV/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.pytype_cache/

# Node (DecapCMS local backend proxy
node_modules/
.npm
.cache/

# Build output — we regenerate these in CI and for local build.
public/
!public/.gitkeep

# PDF outputs are regenerated at build time
# But we keep PDFs that are explicitly committed should be tracked or can be kept as appropriate.
# public/pdfs/*.pdf
# PDFs are regenerated on every build; you may choose to commit if you want to track history but default:
*.log
*.log*

# Editors
.vscode/
.idea/

# Secrets
.env
.env.*
!.env.example

# Cloudflare
.wrangler
worker
.traefik.yml

# Extracted template source
_extracted/
Muhammad-Idris24.github.io-main.zip
